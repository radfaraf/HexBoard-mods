HEXBOARD KEYBOARD DOCUMENTATION

This folder contains the Keyboard-mode documentation set.

Subfolders

- `manuals/`
  Keyboard quick and detailed manuals.
- `requirements/`
  Keyboard behavior written in requirement form.
- `layouts/`
  Keyboard menu and screen layout references.

Confidence note

This is a first-pass reconstruction from the current firmware code and comments
in `src/HexBoard.ino`.

- Treat it as the best current structured reference for Keyboard mode.
- Keep confidence notes short and readable when behavior is inferred.
- Use `Shared setting:` notes when a keyboard-visible setting also affects
  Sequencer mode.
